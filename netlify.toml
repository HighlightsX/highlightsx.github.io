[build]
  command = "npm run build"
  publish = "dist"

# @astrojs/sitemap names its index sitemap-index.xml, but crawlers and Search
# Console both probe /sitemap.xml first.
[[redirects]]
  from = "/sitemap.xml"
  to = "/sitemap-index.xml"
  status = 301
